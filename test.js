const x = 5;
let y = 5;
y += 1;
console.log(y);

let monthlyRent = 500;
const yearlyRent = monthlyRent * 12; 
console.log(yearlyRent)

function max(number1, number2) {
  if (number1 > number2) {
    return number1;
  } else if (number) {
    return number2;
  }
}